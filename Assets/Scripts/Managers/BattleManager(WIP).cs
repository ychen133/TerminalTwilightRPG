/* NAME:            BattleManagerV4.cs
 * AUTHOR:          Yu-Che Cheng (Jeffrey), Emmilio Segovia, Kevin Huang
 * DESCRIPTION:     The script class manages the battle and its canvas when battles are triggered. 
 * 				    It derives from  singleton.
 * REQUIREMENTS:    Base class Singleton.cs must be present.
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using System.Linq;
using System.Threading;
using System;

public class BattleManagerV4 : MonoBehaviour {

    protected BattleManagerV4() { } //null constructor

    public enum states { };

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
